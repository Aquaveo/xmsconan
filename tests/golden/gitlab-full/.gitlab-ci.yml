# Required CI/CD variables:
#   CONAN_LOGIN_USERNAME - Conan remote login username
#   CONAN_PASSWORD       - Conan remote login password
#   AQUAPI_URL           - devpi index URL for wheel uploads
#   AQUAPI_USERNAME      - devpi username for wheel uploads
#   AQUAPI_PASSWORD      - devpi password for wheel uploads
#
# The Conan pair is listed unconditionally: no job here sets it, Conan reads
# it from the environment itself, and every build needs the remote -- not just
# the tag-time deploy. See docs/USAGE.md §10.2.
#
# Every job installs `xmsconan[ci]` from the public Aquaveo index -- the
# extra pins conan, cmake, gcovr and the flake8 plugins in xmsconan's own
# pyproject.toml -- and the tools read the package version from
# CI_COMMIT_TAG themselves (0.0.0 on an untagged pipeline), so no job
# exports a version for the next command. See docs/USAGE.md §10.3.
#
# Generated by xmsconan_ci — do not edit manually.

stages:
  - Build
  - Test
  - Package
  - Deploy
  - Coverage
  - Pages

Conan Build:
  stage: Build
  image: docker.aquaveo.com/aquaveo/conan-docker/conan-gcc13-x11-gdal-py3.13
  variables:
    PYTHON_TARGET_VERSION: '3.13'
  artifacts:
    expire_in: '1d'
    when: always
    paths:
      - .export
      - wheelhouse/
      - test_artifacts/
  script:
    - pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job build --defer-cxx-tests --export

"Run C++ Tests - Release-testing":
  stage: Test
  image: docker.aquaveo.com/aquaveo/conan-docker/conan-gcc13-x11-gdal-py3.13
  except:
    - tags
  needs:
    - job: "Conan Build"
      artifacts: true
  artifacts:
    expire_in: '1d'
    when: always
    reports:
      junit: TEST-cxxtest.xml
    paths:
      - test_artifacts/Release-testing/
      - TEST-cxxtest.xml
  script:
    - pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job test --label Release-testing

"Run C++ Tests - Debug-testing":
  stage: Test
  image: docker.aquaveo.com/aquaveo/conan-docker/conan-gcc13-x11-gdal-py3.13
  except:
    - tags
  needs:
    - job: "Conan Build"
      artifacts: true
  artifacts:
    expire_in: '1d'
    when: always
    reports:
      junit: TEST-cxxtest.xml
    paths:
      - test_artifacts/Debug-testing/
      - TEST-cxxtest.xml
  script:
    - pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job test --label Debug-testing

"Conan Build - Windows":
  stage: Build
  image: GLR-UV
  parallel:
    matrix:
      - PYTHON_TARGET_VERSION: '3.13'
  variables:
    AQUAPI_URL: "https://aquapi.aquaveo.com/aquaveo/dev"
  artifacts:
    expire_in: '1d'
    when: always
    paths:
      - .export
      - test_artifacts/
      - wheelhouse/*.whl
  script:
    - uv --version
    - uv venv --python ${PYTHON_TARGET_VERSION} .venv
    - source .venv/Scripts/activate
    - uv pip install --upgrade "xmsconan[ci]>=2.18.0,<3" --index-url https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job build --export
  tags:
    - WinVM

"Conan Build - Windows VS2019":
  stage: Build
  image: GLR-UV
  parallel:
    matrix:
      - PYTHON_TARGET_VERSION: '3.13'
  artifacts:
    expire_in: '1d'
    when: always
    paths:
      - .export
      - test_artifacts/
  script:
    - uv --version
    - uv venv --python ${PYTHON_TARGET_VERSION} .venv
    - source .venv/Scripts/activate
    - uv pip install --upgrade "xmsconan[ci]>=2.18.0,<3" --index-url https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job build --platform windows_vs2019 --export
  tags:
    - WinVM

Lint:
  stage: Test
  image: python:3.13
  allow_failure: true
  script:
    - pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - pip install -i https://aquapi.aquaveo.com/aquaveo/dev/+simple/ flake8-aquaveo
    - xmsconan job lint

Repair Wheel:
  stage: Package
  image: docker.aquaveo.com/aquaveo/conan-docker/conan-gcc13-x11-gdal-py3.13
  dependencies:
    - Conan Build
  script:
    - /opt/python/cp313-cp313/bin/pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - /opt/python/cp313-cp313/bin/xmsconan job package
  artifacts:
    expire_in: '1d'
    paths:
      - wheelhouse/

"Wheel Deploy":
  stage: Deploy
  image: python:3.13
  dependencies:
    - Repair Wheel
  script:
    - pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job deploy --wheels-only
  only:
    - tags

"Conan Deploy - Linux":
  stage: Deploy
  image: ubuntu:latest
  dependencies:
    - Conan Build
  script:
    - apt-get update
    - apt-get install -y python3-pip python3-venv
    - python3 -m venv .venv
    - source .venv/bin/activate
    - pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job deploy --conan-only
  only:
    - tags

"Wheel Deploy - Windows":
  stage: Deploy
  image: python:3.13
  needs:
    - job: "Conan Build - Windows"
      artifacts: true
  script:
    - pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job deploy --wheels-only
  only:
    - tags

"Conan Deploy - Windows":
  stage: Deploy
  # GLR-UV and `uv venv`, for the reasons spelled out on "Conan Build -
  # Windows" above. This job only restores and uploads a cache tarball, so it
  # does not compile against the interpreter; it still bootstraps its venv the
  # way the build job does so neither of the two can be the one that rots.
  image: GLR-UV
  variables:
    # No `parallel: matrix` any more. Restoring and uploading is
    # ABI-independent, and `needs: artifacts: true` on a fanned-out build hands
    # every instance the whole set of tarballs -- so N deploy instances each
    # restored all N and raced to upload the same Conan reference. The
    # per-instance `--restore` name was what kept them apart, and it is gone
    # now that `job deploy` globs `.export/`. The venv still needs *an*
    # interpreter, so this pins the newest ABI the matrix builds.
    PYTHON_TARGET_VERSION: '3.13'
  # Depend on the whole build job rather than selecting one parallel instance.
  # GitLab does not expand variables inside needs:parallel:matrix, so a selector
  # like "PYTHON_TARGET_VERSION: $PYTHON_TARGET_VERSION" is compared literally,
  # matches no job, and fails the whole pipeline at config time with
  # "undefined need".
  needs:
    - job: "Conan Build - Windows"
      artifacts: true
  script:
    - uv --version
    - uv venv --python ${PYTHON_TARGET_VERSION} .venv
    - source .venv/Scripts/activate
    - uv pip install --upgrade "xmsconan[ci]>=2.18.0,<3" --index-url https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job deploy --conan-only
  only:
    - tags
  tags:
    - WinVM

"Conan Deploy - Windows VS2019":
  stage: Deploy
  image: GLR-UV
  variables:
    # One job, not a fan-out, for the reason given on "Conan Deploy - Windows"
    # above; the pinned ABI is only what the venv is built on.
    PYTHON_TARGET_VERSION: '3.13'
  # The whole build job, not one parallel instance, for the reason given on
  # "Conan Deploy - Windows" above: GitLab does not expand variables inside
  # needs:parallel:matrix.
  needs:
    - job: "Conan Build - Windows VS2019"
      artifacts: true
  script:
    - uv --version
    - uv venv --python ${PYTHON_TARGET_VERSION} .venv
    - source .venv/Scripts/activate
    - uv pip install --upgrade "xmsconan[ci]>=2.18.0,<3" --index-url https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job deploy --conan-only --platform windows_vs2019
  only:
    - tags
  tags:
    - WinVM

"Coverage Build":
  stage: Coverage
  needs: []
  except:
    - tags
  image: docker.aquaveo.com/aquaveo/conan-docker/conan-gcc13-x11-gdal-py3.13
  # The resolved coverage ABI, which also names the conan-gcc13 image above --
  # so on those two branches the container interpreter and the ABI the build
  # targets cannot drift apart. Under an explicit [ci].docker_image the image
  # is whatever the repo named and only this pin is resolved, so keeping that
  # image's interpreter in step is the repo's job. xmsconan_coverage exports
  # the same value itself; declaring it here keeps the job readable without
  # tracing into the packager.
  variables:
    PYTHON_TARGET_VERSION: '3.13'
  script:
    - pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan_conan_setup
    - export CTEST_PARALLEL_LEVEL=${CTEST_PARALLEL_LEVEL:-8}
    - xmsconan_coverage --phase collect build.toml
  artifacts:
    expire_in: '1d'
    when: always
    paths:
      - cov-cpp.xml
      - cov-cpp-summary.json
      - cov-py.xml
      - cov-py-summary.json
      - coverage-status.json
      - coverage-html-cpp/
      - coverage-html-py/

Coverage:
  stage: Coverage
  needs:
    - job: "Coverage Build"
      artifacts: true
  # Under [ci].split_tests the tests gate the pipeline from the "Run C++ Tests"
  # jobs, so a coverage *gate* miss is advisory here. Only that exit code is
  # forgiven. The tool failing -- an exception, the collect job leaving no
  # summary to read -- still fails the job, because `allow_failure: true`
  # forgave those too, and a Coverage stage reporting green while producing no
  # report at all is how a broken coverage run survived an entire release
  # unnoticed.
  allow_failure:
    exit_codes:
      - 3
  except:
    - tags
  image: python:3.13
  script:
    - pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan_coverage --phase report build.toml
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: cov-cpp.xml
    paths:
      - cov-cpp.xml
      - cov-py.xml
  coverage: '/Coverage total: (\d+\.?\d*%)/'

pages:
  stage: Pages
  image: python:3.13
  dependencies:
    - "Coverage Build"
  script:
    - pip install --upgrade "xmsconan[ci]>=2.18.0,<3" -i https://public.aquapi.aquaveo.com/aquaveo/dev/+simple
    - xmsconan job coverage --pages
  artifacts:
    paths:
      - public
  only:
    - master
    - main
